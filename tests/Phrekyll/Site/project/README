This is our mock project for testing.

The tested input directory is `.phrekyll`.
The tested output directory is `public`, which is cleaned between tests.
The pre-compiled resources directory (for comparison) is `expected`.
